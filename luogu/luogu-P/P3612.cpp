#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define int long long

char s[35];

signed main()
{
    cin>>(s+1);
    int len=strlen(s+1);
    int N;cin>>N;
    while(len<N)
    {
        
    }
    return 0;
}